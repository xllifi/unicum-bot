services:
  unicumbot:
    image: unicumbot
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app
    environment:
      NODE_ENV: production
    env_file:
      - .env
    ports:
      - 6480:6480
