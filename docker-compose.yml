services:
  unicumbot:
    image: unicumbot
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./src:/app/src
    environment:
      NODE_ENV: production
    env_file:
      - .env
    ports:
      - 6480:6480
